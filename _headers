# Cloudflare Pages response headers.
# Font files never change once shipped (a new version gets a new name), so they can be cached for a year.
/assets/fonts/*
  Cache-Control: public, max-age=31536000, immutable
